export function Testimonials() {
  // TODO: Ask Heath for 3-6 real quotes from guests/listeners/clients.
  // Expected shape: { quote: string, author: string, role: string }
  // Until then, this section renders nothing rather than fake praise.
  return null;
}
